import React from "react";
// import Timer from "../../helpers/timer";
import "./style.sass";
// import timer from "../../helpers/timer";

const Profile = () => {
  return (
    <div className="profile">{/*<Timer time="02:00:00" paused={true} />*/}</div>
  );
};

export default Profile;
